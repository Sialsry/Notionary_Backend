const { TeamProject } = require("../../models/config")




const createFolder = async(folderName) => {
    try {
        const data = await TeamProject.create({
            
        })
    } catch (error) {
        
    }
}